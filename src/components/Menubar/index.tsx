import { Navigation } from './Navigation';
import { MenubarContainer } from './styles';

const Menubar = () => {
  return (
    <MenubarContainer>
      <Navigation />
    </MenubarContainer>
  );
};

export { Menubar };
